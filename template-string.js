const first = 'kotim';
const second = 'ali';
const oldSum = first + ' ' + second;
// console.log(oldSum);
const newSum = `${first} ${second}`;
// console.log(newSum);
const multiLine = 'this is a first line.' + 'this is a second line';
const multiLineNew = `this is a first line. 
this is a second line.
this is a third line of ${first} ${second}.
`;
console.log(multiLineNew);
