let numbers = ['abul', 'babu', 'sona', 'pona', 'anwar'];

for (let i = 0; i < numbers.length; i++) {
  const number = numbers[i];
  //   console.log(number);
  if (number.length % 2 != 0) {
    element = number;
  }
}
